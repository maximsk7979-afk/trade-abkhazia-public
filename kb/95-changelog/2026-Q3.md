---
title: Changelog 2026 Q3
status: active
last_updated: 2026-07-02
---

# Changelog — 2026 Q3

## 2026-07-02 (вечер) — Ф-2 старт: аудит кассовых ног + браузерная оплата в кассу (шаг 1)

**Аудит исходящих/входящих ног (код + бой, read-only):** касса ловила ~83% приходов и ~0% расходов. Разрывы: (1) **браузерный `/api/v2/sale-direct/save` писал оплату мимо кассы** — 9 walk-in продаж 192 892 ₽ + S-ПС-182 18 320 ₽ без ног (мини-апп — все ноги на месте); (2) `supplierPayments` в карточках ЗК-006 (176 662, без даты) + Р-005 (7 132) — вне settlements и кассы; (3) 10 оплат поставщикам 12–18.06 (20 318) без ног — до шага 2 финблока; (4) оплат контрагентам/партнёру ещё не было (услуги рейсов «Оплатил=Мы» 45 строк / 92 500 — начисления, долги верные); (5) `payCashLeg` не писал ногу `defect_refund`, категория контрагента — жёстко несуществующая `opex`. Остатки кошельков: Голованов 1 065 002 ₽, Арсен 981 283 ₽, СМВ ₾ 29 118.

**Шаг 1 (код) реализован** (план согласован: 1 код → 2 привязки контрагентов → 3 бэкфилл → 4 GAP-059+«Касса»):
- **Браузерная прямая продажа — оплата только с ₽-кошельком** (fail-closed, как `completeClient`): поле «В кошелёк» в форме (оплата>0), префилл при правке (именованный — из settlement `S-<saleId>`, walk-in — из ноги `refType:'sale'`); сервер валидирует кошелёк и пишет ногу **в той же транзакции** (`buildSaleCashLegs` в `sale-direct-service`, идемпотентно по refId; walk-in paid→0 снимает устаревшую ногу). Мини-путь не тронут (нога через `recordClientPayment`).
- **Отмена продажи** снимает ногу settlement-оплаты в транзакции + зовёт общий `cancelInlinePayments` (walk-in/inline, ADR-025); фронт применяет `cashMovements` из ответа.
- **`payCashLeg` («Расчёты»):** + `defect_refund` (направление инвертируется: клиенту — outflow, от поставщика — inflow; гард овердрафта по направлению); категория контрагента — из привязки карточки `payCategories[0]`; партнёр — новая статья **`partner_payment`** (B1, `pnl:false`, заведена в KV, 36→37).
- **+5 тестов** `buildSaleCashLegs` (20 зелёных). Деплой trade-api + фронт (бандл `index-BFjhbJjB.js`). Прод-смоук: отказ без кошелька ✓, ПС-202 нога в КС-003 ✓, отмена снимает ногу и восстанавливает FIFO ✓ (продажа-смоук осталась «Отменена»).
- Коммит `0c79412`. Следующее: шаг 2 — категории/привязки контрагентов под статьи 01.07 (`local_transport`, `rent_vehicle`, `loader_abh`, арендодатели; вопрос владельцу: КА-006 «Грузчики Сухуми» → `loader_abh`?).

## 2026-07-02 — Ретро-усышка распроданных весовых (реклассификация COGS→B2) + проведён баклажан

**Запрос владельца:** списать усышку баклажана (+23 кг в сверке), но в «Усышке» его нет — остаток 0. Решение: модуль дополнен **ретро-усышкой** для распроданных весовых с кг-хвостом товарооборота.

**Бухгалтерская суть:** у распроданных партий стоимость уже целиком в COGS (весовые списываются по книжному весу партии) → списать хвост «сверху» = двойной счёт. Ретро-проводка — **отчётная реклассификация**: строка `retro:true` в СПИС-записи попадает в B2 (через totalCost) и **вычитается из COGS** той же суммой (`pnl-service` → `retroShrinkageReclass`, `shrinkage-service.retroReclass`). Конверсация тождественна по построению: **чистая прибыль не меняется**, валовая очищается, усышка видима строкой. Стоимость кг = средневзвешенный ₽/кг закрытых партий SKU. Излишек (dNetto<0) — контра. Признание — периодом проводки (стандартная коррекция текущим периодом).

**⚠️ Боевой урок (первая реализация откатана):** вариант «уменьшать `nettoOrig` закрытых партий → живой COGS пересчитается» на бою СЛОМАЛ конверсацию (COGS −971 вместо −2 780, чистая −1 809): живой COGS цепочек «погрузка→возврат→перепродажа» опирается на **снимки возвратных партий** — правка родителя доходит не до всех потребителей. Проводка откатана вручную (nettoOrig восстановлены из pre-audit снимка byte-exact, P&L вернулся до копейки), механика переделана на чистую отчётную контру **без мутации партий**. Вывод в копилку ADR-025: ретро-правки живого COGS через партии небезопасны при возвратных цепочках.

**Реализовано:** `computeRetroShrinkage` (без мутаций) + `retroReclass` в `shrinkage-service.js`; retro-строки в `POST /api/v2/shrinkage` (совместимо с обычными в одной проводке; whop `netto` со знаком закрывает кг-хвост сверки); `pnl-service` — `cogsNet −= retroReclass` + коррекция `byProduct`; фронт — секция **«📉 Распроданные весовые»** во вкладке «Склад → ✂️ Усышка» (кг-хвост из `/api/turnover`, предзаполнение). Тесты: shrinkage 7→11, pnl 9 — зелёные. Бандл `index-GQxxFizX.js`.

**Проведено: СПИС-003 — баклажан 23 кг / 2 780,23 ₽** (ретро). Прод-сверка: COGS 1 834 781,58 (−2 780,23), валовая 598 395,82, B2 12 847,16 (баклажан строкой), **чистая 585 548,66 — не изменилась** ✓; кг-хвост баклажана в товарообороте закрыт (списано 23 кг, Δкг 0); партии не тронуты (nettoOrig байт-в-байт). Остальные ретро-хвосты (картофель 40кг +37,3; острый пл.5 +1; контра: картофель 35 −34,5, черри −1,6) — на решение владельца в новой секции.

## 2026-07-02 — Кг-слой усышки в товарообороте (сверка весовых)

Перед первой боевой весовой инвентаризацией (остатки кладовщика) сверка товарооборота получила вес-слой:
- **`turnover-service.js`**: у весовых строк `byProduct[].discrepancyKg = куплено(нетто) − продано(факт-вес отгрузок) − списано(кг из whops writeoff) − остаток(кг из партий)`; тарные = 0. +1 тест (11 зелёные).
- **Фронт «Сверка остатков»**: у весовых вторая строка в кг во всех колонках (куплено/продано/списано/остаток/расхождение); кг-расхождение подсвечивается янтарным. Легенда обновлена: ненулевой кг-хвост по распроданным позициям — историческая усышка (партий нет, не списывается); по позициям с остатком — закрывается проводкой «Склад → ✂️ Усышка».
- Деплой: trade-api + бандл **`index-BNymEx6C.js`**. Прод-картина накопленной весовой усышки (топ по остаткам): лук +93,35 кг, абрикос +30,46, ракета +27,31, острый 25кг +20,7, черешня 15кг +15,88, персик +12,6; отрицательные (перец −21,37, картофель 35 −34,5) — излишки/неточность приёмочного веса, спишутся контрой при вводе факта.

## 2026-07-02 — Аудит «пропавшего ящика банана»: найдены все 6, GAP-077 (fail-open sale-direct) закрыт, коррекция данных

**Запрос владельца:** товарооборот показывал по банану расхождение (100 куплено, 94 продано, склад 0) — найти причину. Полный аудит всех записей (партии/whops/продажи/доставки/рейсы) свёл движение **до единицы**.

**Раскладка 6 нераспроданных ящиков (100 − 94):**
- **3 ящ — ПС-092 (18.06, разовый, оплачено 7 500 ₽):** продажа проведена при книжном остатке 0 (возврат Д-005 ещё не оприходован) → `fifoAllocate` вернул shortage, но продажа записалась **с пустыми аллокациями** (whop `WH-SD-ПС-092-3.allocations=[]`) — товар ушёл без COGS, фантом +3 в книге, позже ошибочно срезан усышкой СПИС-001. → **GAP-077**.
- **2+2 ящ — потери в развозах Д-002 (25/10/13) и Д-005 (40/19/19):** след удалённых «ошибочных» ПС-051/ПС-054 (чистка 21.06) — продажи стёрли, товар не вернулся. Решение владельца: **списываем в усышку**.
- **2 ящ — реальная потеря склада**, корректно накрыта СПИС-001.
- Попутно: **Д-004** `returns` банана = 8 при whop/партии = 11 — битый документ (рассинхрон двух writer'ов return-flow, [GAP-072](../00-meta/gaps.md#gap-072)).

**Сделано:**
1. **[GAP-077](../00-meta/gaps.md#gap-077) закрыт — sale-direct теперь fail-closed:** `doSaleDirectSave` (оба входа: браузер + мини-апп) при `plan.shortages` отклоняет продажу до записи (`withTx`→ROLLBACK) с сообщением «Недостаточно книжного остатка (FIFO)… Сначала оприходуйте возврат/приёмку». Погрузка доставки была fail-closed давно — это был единственный fail-open писатель FIFO.
2. **Миграция `restate-banana.js`** (DRY→бой, бэкап `/var/www/_bak/2026-07-02-banana-restate/`, идемпотентна, партии НЕ тронуты — суммарное выбытие то же): ПС-092 `_fifoAllocs` += B-0029×3 (**COGS 4 956,52 ₽**) + whop-аллокации; **СПИС-001** банан 5→2 ящ (**3 334,66 ₽**, только реальные потери склада) + whop qty 5→2; новая **СПИС-002** «бой в развозе» 4 ящ (**6 732,27 ₽** по себестоимости машин: 2×1698,80 Д-002 + 2×1667,33 Д-005) + whop `WH-SHR-СПИС-002-0`; **Д-004** `returns` 8→11.
3. **Прод-сверка после:** товарооборот банана — куплено 100 / продано 94 / **списано 6 / расхождение 0** (сходится с интуицией владельца «должно быть 6»); P&L: COGS 1 834 825,97 (+4 956,52), валовая 598 351,43, **B2 = 10 066,93** (2 склад + 4 развоз), чистая **588 284,50** (−6 732,27 — честная цена потерь развоза, ранее «испарявшихся» из P&L; чистая по ПС-092-части не изменилась — переклассификация B2↔COGS). Смоук fail-closed: продажа 1 ящ банана при остатке 0 → отказ с подсказкой.

Деплой: trade-api (pm2). Тесты sale-direct/turnover/pnl зелёные. Скрипт миграции сохранён в `code/ops/migrations/`. **Хвост:** конвергенция двух writer'ов return-flow (Д-004-кейс) — [GAP-072](../00-meta/gaps.md#gap-072).

**Полный скан всех SKU (по запросу владельца, той же сессией):** сверка трёх срезов (товарооборот / FIFO-партии / склад-«Движение») по всем позициям + прогон всех whop-shipment (`qty` vs Σ`allocations`). Найден **второй и последний прецедент GAP-077** — **ПС-127** (23.06, К-019): строка чеснока 3 мешка покрыта FIFO лишь на 1 → 2 проданы без списания (книжный остаток чеснока завышен, ТО-расхождение −2). Исправлено `restate-garlic.js` (бэкап `2026-07-02-garlic-restate`): ПС-127 += B-0074×2 (COGS +2 735,83 ₽), B-0074 `qtyRem` 8→6. **Итог: ВСЕ SKU — расхождение 0**; P&L: COGS 1 837 561,81, чистая 585 548,66. Книжный чеснок 26 мест — физический пересчёт кладовщиком при ближайшей сверке. Вес-минусы ТО (черри −1,6 кг, картофель −34,5 кг) — вес-слой, после [GAP-056](../00-meta/gaps.md#gap-056).

## 2026-07-02 — Боевой тест усышки (СПИС-001) + доработка: «Списано» в товарообороте, whop-след, атомарность

**Боевой прогон владельцем** первой проводки усышки — **СПИС-001** (Банан FP карт.20кг, факт 0 мест, списано 5) — со сверкой всех четырёх срезов до копейки (baseline до / снимки после):

- **FIFO ✓** — затронуты ровно 2 партии (B-0029 `qtyRem` 3→0, B-1781940845672-fgs0 2→0), остальные 13 живых партий не тронуты. Стоимость списания **8 291,18 ₽** = точно остаточная FIFO-стоимость (3×1652,17 + 2×1667,33) — инвариант «COGS проданного + списанное = закупка» соблюдён.
- **P&L ✓** — выручка/COGS/валовая (603 307,95 / 24,8%) не тронуты; B2 = 8 291,18 с разбивкой по товару; операционная и чистая упали ровно на эту сумму (595 016,77 / 24,45%).
- **Остатки ✓** — позиция исчезла из stock-service (единый калькулятор → мини-апп/Ева/товарооборот синхронны автоматически).
- **Товарооборот — найден недочёт:** списание падало в «Расхождение» (1→6), формула `куплено − продано − остаток` не знала про списания → колонка теряла смысл (должна ловить только НЕобъяснённые потери).

**Доработка (той же сессией, задеплоено):**
1. **`turnover-service.js`**: сверка читает whops `opType:"writeoff"` (усышка WH-SHR-* + недостачи возвратов WH-LS-* BUG-008) → новая колонка **«Списано»** (`byProduct[].writeoff {qty,netto}`), **`discrepancy = куплено − продано − списано − остаток`**. Фильтр периода/склада — как у закупок/продаж. `/api/turnover` передаёт whops.
2. **Whop-след списания**: `POST /api/v2/shrinkage` пишет складскую операцию `WH-SHR-<СПИС-id>-<i>` (`opType:"writeoff"`, места и/или кг; `wBrutto`=кг для отображения веса) → усышка видна в «Движении по складу». Излишек приходом НЕ рисуем (нельзя оприходовать из воздуха — виден в СПИС-записи и контрой в B2). Фронт синхронизирует whops из ответа (`saveWh`).
3. **Атомарность (паттерн C2.5)**: партии + whops + СПИС-документ — одной транзакцией `withSaleLock(withTx)`; раньше два отдельных вызова (сбой на втором → списание без документа).
4. **Бэкфилл**: whop `WH-SHR-СПИС-001-0` заведён для уже проведённой СПИС-001.

**Тесты**: turnover-service 7→10 (списания-колонка, фильтры, весовая усышка не трогает счёт мест); прогнаны все наборы — зелёные. **Деплой**: trade-api (pm2) + бандл **`index-BIVh5f6Z.js`**; бэкап `/var/www/_bak/2026-07-02-shrinkage-whop/`. **Прод-смоук**: банан — куплено 100 / продано 94 / **списано 5** / остаток 0 / **расхождение 1**; изменилась только строка банана; P&L не изменился.

**Дофикс по снимку владельца:** вкладка «🔄 Движение по товарам» показывала банан «остаток 6 шт» — её синяя колонка была чисто арифметической (купл−прод) и списаний не знала. Добавлена колонка **«Списано ✂️»** и формула **остаток = купл−прод−спис** (банан → 1 шт = необъяснённый хвост; фактический склад 0 — в «Сверке»). Бандл **`index-CcWLZHOe.js`**.

**Роли/Ева (Обязательства 6-7):** новых поверхностей/операций нет (та же вкладка «Товарооборот» owner + «Склад → Усышка»); поле `writeoff` в `/api/turnover` аддитивно — читатели (Ева) не ломаются. **Хвост за владельцем:** расхождение 1 место банана — реальная необъяснённая потеря (была до усышки: 100 куплено, 94 продано, 5 было на складе) — вспомнить/проверить. **Боевой тест покрыл тарную ветку**; весовая (пропорц. `nettoOrig`, контра-излишек) — юнит-тестами, боевой прогон при первой весовой инвентаризации.

## 2026-07-01 — Усышка B2: проводка недостачи/усышки/боя (GAP-054/055 closed, ввод только TradeApp)

**Вариант А (отдельная видимая строка P&L B2, не в COGS).** Ввод — только TradeApp (владелец/офис); мини-апп/Ева не проводят (решение владельца).

- **`trade-api/shrinkage-service.js`** (новый канон): `computeShrinkage` — по факт-остатку веса считает недостачу и списывает по FIFO: **места** (бой/пропажа) → `qtyRem` через `cogsOfBatchUnits`; **вес** (усышка) → уменьшает `nettoOrig` партий (пропорц. нетто-остатку, ₽/кг). **Излишек** (факт > книги) — контра (нетто ↑, минус к B2; решение владельца: итог B2 = недостача − излишки). Тарные — только места. `shrinkageTotal`/`shrinkageByProduct` — для P&L. Конверсация: усохшие кг → B2, `nettoOrig` ↓ → COGS не задваивает.
- **Эндпоинты** `POST /api/v2/shrinkage` (списывает партии под `withSaleLock` + пишет запись `inventory_counts` `kind:"shrinkage"` + возвращает свежие партии для локальной синхронизации), `GET /api/v2/shrinkage` (история). `pnl-service` B2 = Σ этих записей за период (нетто); `breakdown.B2` — по товарам.
- **TradeApp вкладка «Склад → ✂️ Усышка»**: таблица с книжным остатком, ввод факт-остатка, предпросмотр Δ, проводка → списание + P&L; история проводок. Хелпер `apiPostJSON`. Общий `doInventorySave` (места-сверка мини-аппа) **не тронут**. **Правка 2026-07-02:** показаны **ВСЕ позиции** (не только весовые) — тарные/штучные списываются по местам (бой/порча), весовые по весу; бейдж тип-позиции, ввод разведён (весовые — кг, тарные — места). Бандл `index-C34k7AgI.js`.
- **+7 юнит-тестов** усышки + 1 P&L (45 всего зелёные). Деплой: `shrinkage-service.js`/`pnl-service.js`/`server.js` + бандл **`index-C-ymlVtg.js`**. [GAP-054](../00-meta/gaps.md#gap-054)/[GAP-055](../00-meta/gaps.md#gap-055) closed. Прод: эндпоинты 200, B2=0 (проводок ещё нет — владелец введёт факт-остаток).

## 2026-07-01 — Финблок шаг 5: P&L / Ф-3 (движок `pnl-service.js`, GAP-076 closed)

**Капстон-отчёт: прибыль за период, два уровня.** COGS = **себестоимость проданного** (FIFO cost-of-sold), не процюремент — метод начисления.

- **`trade-api/pnl-service.js`** (новый канон, ADR-025 — переиспользует, не дублирует): `soldCogs` (FIFO-себестоимость проданного: sale-direct → `cogsOfBatchUnits` по `_fifoAllocs` продажи; через доставку → `avgCostByPos(dlv._fifoAllocs)` × доля проданной строки — та же формула, что оценивает возвраты в `completeDelivery`), `pnlJournal` (классификатор статей), `pnl` (сборка). **A1 выручка** = `turnover-service`; **B1 COGS** = `soldCogs` (логистика рейса уже в себестоимости партии → в B1, из opex исключена); **B1 контра** = `supplier_refund` (`dir:in`) вычитается; **B2 усышка** = 0 плейсхолдер; **B3 opex** = `delivery.expenses` (развоз) + начисления settlements; **A2/B4** — журнал.
- **Признание без двойного счёта:** accrual-статьи (`party:true`) → начисление (settlements charge); cash-статьи (`CASH_BASIS`) → оплата (cash_movements). Экспортированы `avgCostByPos` (delivery-service) + `saleLineNetto` (turnover-service) для переиспользования.
- **Два уровня waterfall:** выручка − COGS(нетто) = **валовая прибыль** (маржа проекта); − усышка − opex = операционная; + прочие доходы − финрасходы = **чистая**.
- **Эндпоинт `/api/pnl`** (`from`/`to`/`office`) + вкладка **«💹 Прибыль (P&L)»** в TradeApp (waterfall 2 уровня + разбивка статей + COGS по товарам). **+8 юнит-тестов** (37 всего зелёные).
- **Проверено на бою (всё время):** выручка **2 433 177 ₽**, COGS проданного **1 829 869 ₽**, валовая прибыль **603 308 ₽ (24.8%)**; opex/A2/B4 = 0 (операций ещё нет). Сверка: revenue = turnover.sales.total; COGS-проданного (1.83M) < закупки (2.04M) на 207К = стоимость остатка по FIFO. Деплой: `pnl-service.js` + бандл **`index-CHklzwbC.js`**.
- **[GAP-076](../00-meta/gaps.md#gap-076) closed** (delivery.expenses в P&L ровно один раз). **Осталось в Ф-3:** усышка B2 после инвентаризации ([GAP-056](../00-meta/gaps.md#gap-056)); P&L в Еве/мини-аппе на тот же эндпоинт.

## 2026-07-01 — Финблок шаг 3: канонический баланс контрагентов по расходам (GAP-075 closed)

**Решение владельца:** баланс с контрагентом — **вычисляемый** из источника (как балансы Покупателей/Поставщиков, ADR-025), а не отдельными записями-начислениями. Все расчёты в одном месте, без дублей.

- **`trade-api/expense-service.js`** (новый канон): `contragentLedger(contId, data)` + `contragentBalances(data)` — перенос фронтового `getExpenseSummary` 1-в-1. Модель: `trip.expenses` `Оплатил=Мы` → долг перед контрагентом (charged, paid 0); `Оплатил=Партнёр` → контрагенту заплатила Лия (charged=paid, баланс 0), наше обязательство перед Лией — отдельная Лия-ветка (не тронута, распределение по нетто-долям веса верно). Плюс `delivery.expenses` (charged=paid) и ручные `settlements`. Курс `rate=1000/ri`.
- **Эндпоинты** `/api/contragents/summary` + `/api/contragent/:id/summary` (как `/api/suppliers/summary`). «Расчёты → Расходы» (`SettlementsMod`) делегирует на сервер (useEffect+apiGetJSON), **дубль-формулы `getExpenseSummary`/`getExpenseDetail` удалены с фронта**. Оплата контрагенту — через единую форму (шаг 2): кошелёк + расходная нога, гасит вычисляемый долг.
- **+6 юнит-тестов** `expense-service` (29 всего зелёные). Деплой: `expense-service.js` на VPS + бандл **`index-DROmshLv.js`**. Сверено на бою: Топсон 75440/34440/**41000**, Джамна **35000**, Алико **12800**, Махо **5797**.
- **[GAP-076](../00-meta/gaps.md#gap-076) частично:** `delivery.expenses` теперь в едином расчёте контрагентов; P&L-сторона (opex один раз) — в шаге 5.

## 2026-07-01 — Финблок шаг 2: обязательный кошелёк при оплате + касса наличного рынка

**Модель (решение владельца):** «начисление ≠ оплата» — кошелёк нужен только в момент ОПЛАТЫ; при начислении обязательства не нужен. Все оплаты проводятся через **единую форму «Расчёты»** (Вариант 1); карточка рейса/ЗК = только начисление долга.

**Часть A — обычные поставщики (и Лия/расходы):**
- Форма оплаты (`SettlementsMod`, все вкладки): выбор кошелька фильтруется **по валюте платежа** + **только непустые** (баланс>0) + показывает остаток; **гард овердрафта** — исходящая оплата, уводящая кошелёк в минус, блокируется (с корректной обработкой правки существующей оплаты). Механика `payCashLeg` (outflow) уже была — добавлены фильтры и защита.
- Карточки **рейса** и **местной закупки (ЗК)**: ручной редактор `supplierPayments` заменён на **режим просмотра** (оплата — в «Расчёты → Поставщики» с кошельком). Данные/история сохранены.

**Часть B — «Рынок (наличные)» (cashMarket, П-001):**
- Оплата 100% наличными сразу при закупке теперь пишет **расходную ногу кассы** (`recordCashOutflow`) из ₾-кошелька закупщика (`holderType=staff, currency=GEL`), идемпотентно по `refId=tripId/mpId`. Раньше cashMarket-оплата шла мимо кассы.
- **Без жёсткого блока в поле** (деньги физически ушли): если у закупщика нет ₾-кошелька или остаток уходит в минус — покупка проходит, но возвращается `cashWarn` (сигнал пополнить). Владелец финансирует кошелёк заранее; обмен ₽→₾/выдача — [GAP-059](../00-meta/gaps.md#gap-059) после финблока.

**Деплой:** `server.js` (+`recordCashOutflow`, вызов в `/buyer/load`) + `trade_app_v3.jsx`→App.jsx; бандл **`index-BVjBhvr6.js`**; trade-api рестарт (чисто). Бэкапы `.bak-2026-07-01-step2`. Все 23 теста trade-api зелёные.

**Хвосты шага 2 — закрыты (деплой `index-BExF0s2k.js`):**
- ✅ `completeClient` (TradeApp отгрузка): в форме отгрузки — выбор ₽-кошелька (обязателен при оплате>0); `completeClient(saleId, paid, walletId)` пишет `walletId` в settlement-оплату + приходную ногу (inflow) в кошелёк (как мини-апп развоз). `DeliveryMod` получил `cashMov`/`saveCashMov`.
- ✅ мини-апп «Местная закупка»: оплата поставщику при закупке (`lpPay`→`supplierPayments`) теперь пишет расходную ногу кассы (`recordCashOutflow`) из кошелька проводящего (валюта проекта), идемпотентно по `purchaseId`; `cashWarn` при отсутствии кошелька/минусе. `recordCashOutflow` параметризован по валюте.

**Далее:** шаг 3 (вторая нога услуг рейса — кредиторка перевозчика, [GAP-075](../00-meta/gaps.md#gap-075)), шаг 4 (`delivery.expenses` в журнал, [GAP-076](../00-meta/gaps.md#gap-076)), шаг 5 (P&L Ф-3 по `pnl:true`).

## 2026-07-01 — Финблок: свод правил учёта по статьям (A/B/C) + реализация шага 1 (pnl/ord)

**Контекст:** работа в роли финансового аудитора — по каждой статье справочника `trade-cat-paycategories` определены правила заполнения/внесения/учёта. Раздел базы: [`kb/20-domain/financial-reporting/accounting-rules/`](../20-domain/financial-reporting/accounting-rules/) ([rulebook](../20-domain/financial-reporting/accounting-rules/pay-categories-rulebook.md), [журнал](../20-domain/financial-reporting/accounting-rules/decisions-log.md)).

**Согласовано (все 3 группы):** A доходы (A1 `client_payment` +контра `client_overpay_refund`; A2 бонус/НДС/проценты/прочий +контра `supplier_overpay_refund`); B расходы (B1 10 статей — `supplier_payment`, `supplier_refund` contra-COGS, услуги рейса тип-1 ×8 вкл. `loader_abh`; B2 `shrinkage` списание; B3 opex две модели — начисление+баланс/кассовый + `local_transport`/`rent_vehicle`; B4 финрасходы спящие); C вне P&L (капитал владельца активен, займы спящие).

**Заведены 2 GAP:** [GAP-075](../00-meta/gaps.md#gap-075) (кредиторка перевозчика рейса не создаётся, только Лия), [GAP-076](../00-meta/gaps.md#gap-076) (`delivery.expenses` — параллельный реестр мимо журнала/P&L, ADR-025).

**Реализация шаг 1 (справочник статей):**
1. **Миграция KV** `trade-cat-paycategories`: у всех статей добавлены **`pnl`** (в P&L?) и **`ord`** (порядок в группе); метки/`party`/`src` приведены к своду; **+5 новых** статей (`client_overpay_refund`, `supplier_overpay_refund`, `local_transport`, `rent_vehicle`, `loader_abh`) → 31→**36 статей**. `pnl:false` у 17 (баланс/касса: поступления, оплаты долгов, услуги рейса, капитал, займы, возвраты переплат). Капитал C1 — `party:false` (`owner` в системе не поддержан). Бэкап KV — job tmp.
2. **Фронт** (`trade_app_v3.jsx`→App.jsx): дефолт `PAY_CATEGORIES` синхронизирован; форма статьи — селект «Участвует в P&L?» + поле «Порядок в группе»; таблица — колонка «P&L» (В P&L/вне) + `ord` в коде; сортировка внутри группы по `ord`; `savePayCat` нормализует pnl(bool)/ord(int). Деплой бандл **`index-DSfQ1Krd.js`**; бэкап `App.jsx.bak-2026-07-01-paycats` на VPS.

**Далее по финблоку:** шаг 2 (обязательный кошелёк-источник в оплатах), шаг 3 (вторая нога расходов — GAP-075), шаг 4 (delivery.expenses в журнал — GAP-076), шаг 5 (P&L Ф-3 по `pnl:true`).

## 2026-07-01 — Аудит и синхронизация раздела «Касса» (единое ядро + бэкфилл вторых ног)

**Запрос владельца:** проверить «Кассу» — единый ли калькулятор, синхронны ли интерфейсы, нет ли дублей/хвостов/разъехавшихся полей.

**Аудит (данные + код):** структурно чисто — нет дублей id, битых/безногих движений, сирот-кошельков, рассинхрона валют. Найдено: (1) браузерная «Касса» считала остаток своей копией формулы (`trade_app_calc.cashBalances`), отдельной от `cash-service` — численно совпадала, но дубль (дрейф по ADR-025); (2) `recordCashInflow` не идемпотентна (`id='ДД-'+Date.now()`); (3) 1 **сирота** — нога `ДД-1782059378374` (10 000 ₽) на удалённый settlement `S-1782059485871`; (4) **824 724 ₽** клиентских оплат (68 из 132) без второй (кассовой) ноги (фича с ~20.06 + сотрудники без ₽-кошелька) — незавершённость Ф-2.

**Сделано:**
1. **Единое ядро `cash-core.cjs`** (как `trip-cost-core`): остаток кошелька считается в одном месте; `cash-service.js` и фронтовый `trade_app_calc.cashBalances` — делегаты. Паритет с прод-данными ✓, цифры не изменились. Деплой: ядро в `/var/www/trade/` и `frontend/src/trade-api/`, фронт-бандл `index-BR6bxMNe.js`.
2. **Идемпотентность `recordCashInflow`**: при наличии `refId` переиспользуется id существующей ноги (как браузерный `payCashLeg`) → ретрай/повтор не плодит дубль. Деплой trade-api (pm2).
3. **Синхронизация касса↔расчёты** (миграция `backfill-cash-legs.js`, бэкап `/var/www/_bak/2026-07-01-cash-backfill/`, DRY→боевой, идемпотентна): удалена 1 сирота; заведены **68 недостающих вторых ног в кассу Ивана Голованова (КС-003)** — 65 приходов (818 274 ₽) + 3 возврата брака (6 450 ₽), дата/сумма = ровно как в settlement (решение владельца). Итог: сирот **0**, settlements клиентов без ноги **0** — полный синхрон. КС-003: 263 178 → **1 065 002 ₽**.

**Инварианты (проверено на бою):** кассовые движения — отдельная проекция, `balance-service`/KPI/товарооборот их не читают → **балансы клиентов = 190 468,9 НЕ изменились**; дублей `refId` нет; Ева и мини-апп читают кассу через канон `cash-service`. Осталось на Ф-2: кассовые `outflow` для расходов/оплат поставщикам, строгая валидация `validateDoc`, дедуп полей `name`/`desc`.

## 2026-07-01 — Фикс KPI «Продажи»: долг сведён с «Расчётами» (единый калькулятор)

**Симптом (аудит по двум снимкам владельца):** вкладка «Продажи» показывала долг **636 607,9 ₽**, вкладка «Расчёты → Клиенты» — **190 468,9 ₽**. Расхождение **446 139,0 ₽**.

**Аудит на боевых данных (свёл до рубля):** правильное число — **190 468,9** (канон `balance-service`, GAP-058). KPI-карточки «Продажи» (Выручка/Оплачено/Долг) — отдельный расчёт, разошедшийся с каноном (дрейф ADR-025). Две причины:
1. **394 955,0 ₽** — «Оплачено» считалось только по журналу `settlements` и НЕ включало `saleData.paid`. Оплаты разовых (`__walk_in__`) лежат именно там (settlement для walk-in не создаётся) → разовые выглядели неоплаченными.
2. **51 184,0 ₽** — в «Выручку» попадали **отменённые** продажи (5 шт: ПС-038/039/091/116/122), которые `balance-service` исключает.
394 955,0 + 51 184,0 = 446 139,0 (точно).

**Фикс** (`trade_app_v3.jsx`, SalesMod, KPI-блок): (а) отменённые исключены из выручки; (б) «Оплачено» = `salePaid` продажи (`saleData.paid` у sale-direct / `shipmentPaid` у заявок) + платежи/возмещения из `settlements` — как в `balance-service`. Проверка на боевых данных: Выручка 2 295 101,9 − Оплачено 2 104 633,0 = **Долг 190 468,9** = «Расчёты» ✓. Только отображение — продовые данные не менялись.

**Аудит Евы и мини-аппа (по запросу владельца):** подтверждено — баланс/долг везде через единый `balance-service` (Ева по HTTP `/api/client/:id/summary`; мини-апп `clientBalance`/`clientsRoster`; админка «Расчёты» — тоже). **Двойного учёта оплат нет**: walk-in → только `saleData.paid`, именованный → только settlement; поля не пересекаются, откат оплат синхронизирован. Единственный неканон — этот KPI «Продажи» — устранён.

**Деплой:** trade-app (бандл `index-CNVRwDAo.js`). Бэкенд/данные не тронуты. Без GAP (решение владельца — правим сразу).
